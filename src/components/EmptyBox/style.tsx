import { Box, styled } from '@mui/material';

export const Container = styled(Box)({
  width: ' 100%',
  height: ' 200px',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  fontSize: '40px',
});
